#### 痛点
- [ ] 机器翻译
- [ ] 通行证只能手动  -针对氪金玩家


#### 爽点



这个现象更像是**本地会话/模型会话状态异常**，而不是网络完全不可用。你给的现象（点发送后一直转圈）结合我看到的日志，优先级高的嫌疑是这几个：

1. 应用中存在**多个 Codex 进程**，可能有旧实例残留，导致会话状态互相干扰。
2. 启动日志里有“未知 conversation”的开始/完成事件，说明本地会话状态可能脏掉了。
3. 日志里出现 `gpt-5.3-codex-spark`，与你期望的 `GPT5.5` 不一致，可能是更新后模型默认别名/映射回退了。
4. 还有一次 `workspace_type` / `brand_name` 空值导致的 profile/bootstrap 报错，提示配置链路有异常。

建议你按“无损恢复”顺序处理（先不删文件，先改名备份）：

1. 完全退出并清理进程
    - 任务管理器里关闭所有 `Codex.exe`，或在 PowerShell 里执行：
        
        ```
        Stop-Process -Name Codex -ErrorAction SilentlyContinue
        ```
        
2. 备份并重建本地配置（可恢复）
    - 关闭应用后执行：
        
        ```
        $u = "$env:LOCALAPPDATA\OpenAI\Codex"
        $p = "$env:LOCALAPPDATA\Packages\OpenAI.Codex_2p2nqsd0c76g0\LocalCache\Local\Codex"
        Rename-Item $u  "$($u)_bak_$(Get-Date -Format yyyyMMdd_HHmmss)" -ErrorAction SilentlyContinue
        Rename-Item $p  "$($p)_bak_$(Get-Date -Format yyyyMMdd_HHmmss)" -ErrorAction SilentlyContinue
        ```
        
3. 重新启动 Codex，进设置里手动重新选模型
    - 先别选 5.5，先选“可用的官方模型”再试一次，确认能发消息成功后再切回 5.5。
    - 如果界面支持，切到默认模型（如 `gpt-5.3-codex-spark`）做一次通路验证。
4. 检查版本一致性（避免“看着更新了但没真正切换”）

- 运行：
    
    ```
    Get-AppxPackage OpenAI.Codex | Select-Object Name,Version,PackageFullName
    ```
    
    看看当前实际在用的版本是否和你预期一致（你提的 `26.611.61753` 与我看到的包路径 `26.611.8273.0` 有差异，说明实际加载版本可能并非想象中的那个）。

如果按上面做完仍卡住，我建议再做一轮：重启系统后，只保留一个 Codex 实例再测，同时把最近 20 分钟日志文件贴来（尤其是含 `Received turn/started`、`bootstrapping`、`error` 的片段），我可以继续给你精确定位到是哪一段导致卡死。  
如果你愿意，我可以继续按这个顺序替你一步步执行命令并给你看每一步结果。