
大致内容：
1. 项目类型  
   `D:\SVN` 是混合 Roblox 工程：  
   [default.project.json](D:/SVN/default.project.json:1) + [src](D:/SVN/src) 是 Rojo 源码；[place/CaseProject.rbxl](D:/SVN/place/CaseProject.rbxl) 保存 Studio 场景、Resource、UI、模型、特效等非源码资源；`config/*.xlsx` + `makedata` + `assist` 是配置导出和 FileList 生成链路。

2. 启动链路  
   服务端入口：[ServerStart.server.lua](D:/SVN/src/ServerScriptService/ServerStart.server.lua:27)  
   客户端入口：[ClientStart.client.lua](D:/SVN/src/ReplicatedFirst/ClientStart.client.lua:22)  
   双端都先加载 `CommonHotpot`，再加载 `ServerHotpot` / `ClientHotpot`，然后创建 `GameServer` / `GameClient`。

3. 核心玩法链路  
   玩家登录后客户端创建 `Client_Player`，拿 `getPlayerGuid`，加载组件，然后角色进入时自动执行 `LoadStage(1)`：  
   [Client_Player.lua](D:/SVN/src/ReplicatedStorage/Script/Client/Object/Client_Player.lua:16)  
   [Client_Player.lua](D:/SVN/src/ReplicatedStorage/Script/Client/Object/Client_Player.lua:33)

   关卡进入通过 `RemoteFunction in_stage`：  
   [Client_StageComponent.lua](D:/SVN/src/ReplicatedStorage/Script/Client/Component/Client_StageComponent.lua:31)  
   [Server_StageComponent.lua](D:/SVN/src/ServerStorage/Script/Server/Component/Server_StageComponent.lua:143)

   普通攻击链路是：客户端子弹碰到怪物 collider -> `player_attack_monster` -> 服务端扣血 -> 广播 `monster_be_dmg`：  
   [Client_PlayerCharacter.lua](D:/SVN/src/ReplicatedStorage/Script/Client/Object/Client_PlayerCharacter.lua:142)  
   [Server_StageObj.lua](D:/SVN/src/ServerStorage/Script/Server/Object/Server_StageObj.lua:88)  
   当前普通攻击服务端写死伤害 `10`。

4. 已验证状态  
   `rojo sourcemap default.project.json --output NUL` 成功。  
   `rojo`、`aftman`、`stylua`、`selene` 可用。  
   `lua` 不在 PATH，所以 `assist/reload.lua` 和 `config_mark.lua` 现在不能直接跑。  
   `svn` 当前可用，版本是 `1.14.2-SlikSvn`。这点和旧接手文档记录不同，以当前验证为准。

5. 主要风险  
   [ServerFunctionManager.lua](D:/SVN/src/ServerStorage/Hotpot/Server/Module/ServerFunctionManager.lua:123) 有明显语法残缺：`local result remote_function:InvokeClient(...)`。  
   [Server_PlayerObject.lua](D:/SVN/src/ServerStorage/Hotpot/Server/Object/Server_PlayerObject.lua:2) 有 `local assert = asser` 拼写问题。  
   技能伤害依赖 `player.prop_cmp` 和 `player.skill_cmp`，但 `Server_Player` 当前只加载 `sync_cmp`、`stage_cmp`、`bag_cmp`。  
   [Server_StageObj.lua](D:/SVN/src/ServerStorage/Script/Server/Object/Server_StageObj.lua:132)  
   [Server_Player.lua](D:/SVN/src/ServerStorage/Script/Server/Object/Server_Player.lua:38)

   客户端还有旧系统残留：`teleport_gui`、`tips_gui`、`stageDrop`、`relics`、`christmasMonsterCfg`、`weatherCfg`、Boss HUD 方法等，不应默认认为这些功能完整。

6. 还缺的关键资料  
   `Project_Analysis_Package/Audit_*.md` 都是空的，所以 `.rbxl` 内的 `Resource.Stage.stage1`、`Resource.monster`、`Resource.ui`、`pfb`、动画/音效对象还没有导出。源码我已掌握到主链路层面，但要完全掌握 Studio 资源结构，下一步必须跑导出。

