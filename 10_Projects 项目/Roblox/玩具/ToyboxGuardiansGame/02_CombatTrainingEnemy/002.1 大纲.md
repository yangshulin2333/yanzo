
`002.1_CombatTrainingEnemyQualityPass / 训练怪质量补强`

目的不是加几个效果，而是把 002 从“能跑通数据链路”补成“第一个真实玩法 Feature 可验收”。

这次边界我建议这样定：

|范围|是否进 002.1|说明|
|---|---|---|
|R15 训练假人|进|M001 必须继续是角色模型，不接受 Part 占位验收|
|头顶血条|进|从纯 `20 / 20` 升级为可复用世界血条：填充条 + 数字|
|掉落物模型|进|SmallGear 必须是可识别掉落物，不能再是抽象球/SurfaceGui|
|掉落落地过程|进|生成后先落地/停顿，再进入可拾取状态|
|吸走表现|进，但模块化|做成 `PickupMotion` 可复用表现模块，后续磁铁玩家/磁铁玩具直接复用|
|SmallGear / Exp 临时反馈|进|没正式 HUD 前也要让玩家看到 `+1 SmallGear`、`+4 Exp`|
|正式 HUD|不进|归属 `003_PlayerHUD`|
|Exp 等级曲线|不进|归属 `005_ExpLevelProgression`|
|普通怪材料|不进 002.1|已提前到拼装台前，归属 `006_BasicMaterialEnemies`|
|怪物追击/攻击/玩家死亡|不进 M001|可单独做 `002.1C_BasicThreatProbe`，但不塞进训练假人|

**吸走模块边界**

吸走可以做，但要拆成两层：

Server 仍然只负责权威判定：

- 是否掉落。
- 谁拥有掉落。
- 什么时候可拾取。
- 玩家是否进入有效拾取范围。
- 是否写入 `Resources.SmallGear` 和 `Progression.Exp`。

Client 只负责表现：

- 掉落物抛出。
- 落地。
- 待机轻微浮动。
- 拾取时飞向目标。
- 消失和奖励文字。

后续磁铁玩家要复用时，只换“吸向谁”和“什么条件触发”，不改动画核心。

建议运行文件边界：

- 新增表现模块：`D:\AI\Codex\ToyBox\ToyboxGuardiansGame\src\StarterPlayer\StarterPlayerScripts\Client\Game\Presentation\PickupMotion.lua`
- 改造掉落控制器：`D:\AI\Codex\ToyBox\ToyboxGuardiansGame\src\StarterPlayer\StarterPlayerScripts\Client\Game\Controllers\LootDropController.lua`
- 新增表现配置：`D:\AI\Codex\ToyBox\ToyboxGuardiansGame\src\ReplicatedStorage\Game\Shared\Config\LootPresentationConfig.lua`
- 服务端新增可拾取时间：`D:\AI\Codex\ToyBox\ToyboxGuardiansGame\src\ServerScriptService\Server\Game\Services\LootDropService.lua`
- 掉落逻辑参数仍在：`D:\AI\Codex\ToyBox\ToyboxGuardiansGame\src\ReplicatedStorage\Game\Shared\Config\LootTableConfig.lua`

`PickupCheckIntervalSeconds` 只管服务端多久检查一次距离；真正“落地后再能捡”应该新增 `PickupReadyDelaySeconds` 或 `pickupReadyAt`，不能拿扫描间隔硬凑。

**002.1 子模块**

1. `002.1A EnemyPresentation / 训练怪表现补强`  
    做血条、受击反馈、死亡状态、刷新反馈。验收不是 Output，而是玩家能看懂敌人状态。
    
2. `002.1B LootDropPresentation / 掉落物表现补强`  
    做 SmallGear 可识别模型、落地、待机、拾取吸走、过期消失。
    
3. `002.1C RewardFeedbackBridge / 奖励临时反馈`  
    正式 HUD 前，至少显示本地 `+1 SmallGear` 和 `+4 Exp`。这不是完整 HUD，只是反馈桥。
    
4. `002.1D WorkflowRepair / 文档和验收修复`  
    更新 002 父级卡，标记原 002C 为 `Implemented but Quality Not Accepted`，补齐“不做事项归属表”，以后不能只写“不做”。
    

**明确验收标准**

002.1 完成后，玩家必须能看到：

- M001 是 R15 训练假人。
- 头顶血条有填充变化，不只是 `20 / 20` 文本。
- 攻击后有受击反馈。
- 死亡状态明确。
- SmallGear 是齿轮/玩具零件形象，不是抽象球。
- 掉落物先落地，再可拾取。
- 拾取时可以吸向玩家，但这是表现模块，不代表 Client 决定奖励。
- 拾取后显示 `+1 SmallGear` 和 `+4 Exp`。
- 多人时私有掉落仍只属于击杀者。
- Server 权威不被破坏。

下一步我应该先落文档，不写代码：创建 `002.1` Feature Card、TDD、UI/模型契约，并把 `继续ToyBox` 口令修进项目入口文档。你确认后我按这个边界开始写。