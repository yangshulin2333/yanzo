#### 深拷贝
```lua
local origin{}

function deepClone(origin){
	local copy = {}
	for k,v inpairs (origin){
		if typeof(v) == tables then
			copy[k] = deepclone(v)
		else
			copy(k) = copy(v)
		end
		return copy
		 
	}
end
}
```